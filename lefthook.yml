pre-commit:
  parallel: true
  commands:
    standard:
      glob: "*.rb"
      run: bundle exec standardrb --fix {staged_files}
      stage_fixed: true
    reek:
      glob: "*.rb"
      exclude: "examples/"
      run: bundle exec reek {staged_files}
